* wlp-engine

The easiest way to install and use this library 
is to use the stack library tool and compile it
with it.

After compiling, with the command stack ghci you
will be droped into a live ghci session to check
the examples and provided functions.
